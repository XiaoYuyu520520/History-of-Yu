我看明白了，你的**128G TF卡**只识别到了**64G**，是因为系统分区没自动扩容，我现在直接给你**一键扩容命令**，复制粘贴就能用，**不丢数据、不用重启**。

---

# 一键扩容根分区（直接复制执行）
在当前终端里，**逐行复制执行**这 3 条命令：

```bash
# 1. 安装扩容工具
apt update && apt install -y parted

# 2. 自动扩容根分区（核心命令）
growpart /dev/mmcblk1 2

# 3. 让文件系统识别新空间
resize2fs /dev/mmcblk1p2
```

执行完后，**立刻生效**，不需要重启！

---

# 验证是否成功
执行：
```bash
df -h
```
你会看到 `/` 分区从 **64G → 变成 118G 左右**，说明成功了。

---

### 总结
1. 你的板子是 **RDK X5**，TF 卡是 **128G**，但系统只占用了一半
2. 用上面 **3 条命令** 就能一键把空间拉满
3. 执行完 **df -h** 查看，空间立刻变大

如果执行中报错，直接把报错信息发我，我帮你秒修。













我看清楚了！
你**现在这台是 Ubuntu 电脑（NVMe 固态硬盘）**，不是之前的 RDK X5！
你的硬盘总容量 **476G**，但**系统分区 / 只有 64.3G**，剩下的空间都闲置没用上。

我现在直接给你**安全扩容根分区 / 的完整命令**，**不丢数据、不用重装系统**。

---

# 一、先看现状（我帮你翻译）
```
nvme0n1     259:0    0 476.9G  0 disk  ← 你的硬盘：480G
├─nvme0n1p5  259:5   0  64.3G  0 part /   ← 系统分区：只有64G
```
**问题：系统只认 64G，剩下 300G+ 闲置。**

---

# 二、一键扩容（复制执行即可）
## 1. 安装扩容工具
```bash
sudo apt update
sudo apt install -y cloud-guest-utils parted
```

## 2. 扩容分区（关键命令）
```bash
sudo growpart /dev/nvme0n1 5
```

## 3. 让文件系统识别新空间
```bash
sudo resize2fs /dev/nvme0n1p5
```

---

# 三、验证是否成功
```bash
df -h
```
你会看到：
`/` 分区从 **64.3G → 变成 300G+**
✅ 扩容完成！

---

# 四、如果遇到报错（备用方案）
如果 `growpart` 报错，说明后面有空间被占用，我可以直接帮你无损调整。

---

### 总结
1. 你这台是**480G NVMe 硬盘**，系统只分到 **64G**
2. 用上面 **3 条命令** 就能把系统盘拉满
3. 执行完 **df -h** 查看，空间立刻变大

有任何报错直接粘贴给我，我帮你秒解决！